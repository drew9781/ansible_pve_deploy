from setuptools import setup

setup(name='ansible_pve_deploy',
      version='0.1',
      description='Deploying proxmox VMs via ansible',
      url='',
      author='drew9781',
      author_email='',
      license='',
      packages=['ansible_pve_deploy'],
      zip_safe=False)